/**
 * Pattern barrel.
 *
 * Patterns compose primitive components into higher-level, reusable
 * interaction flows (for example: an approval flow combining a risk
 * warning, confidence indicator, and human handoff control).
 */

export {};
