/**
 * Template barrel.
 *
 * Templates are complete, presentational AI product surfaces composed from the
 * library's components. They accept typed, demo-ready data through props and
 * emit events. The shared demo data used by their examples and stories lives in
 * `./mockData` and is intentionally not re-exported here, so it is never shipped.
 */

export * from "./CustomerSupportAgentTemplate";
